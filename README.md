# starter-cache

```
Agora que o cache está ativado, você pode executá-lo novamente e ver a diferença adicionando chamadas adicionais
com ou sem o mesmo isbn. Deve fazer uma diferença enorme.

ou

desative o cache nas classses abaixo  para ver a diferença
comentando as seguintes anotações do spring com //

ex:
SpringCacheApplication
//@EnableCaching

e

SimpleBookRepository
//@Cacheable("books")

```